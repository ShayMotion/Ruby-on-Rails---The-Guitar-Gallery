<h1>Add a Drum to an Auction</h1>

<% if @drum.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@drum.errors.count, "error") %> prohibited this drum from being saved:</h2>

    <ul>
    <% @drum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
    <% end %>
    </ul>
</div>
<% end %>

<%= form_for [current_user, @drum] do |f| %>
    
    <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: "form-control" %>
    </div>
    

    <div class="form-group">
        <%= f.label :auction_id, "Select an Auction:" %>
        <%= f.collection_select :auction_id, @auctions, :id, :title, {}, class: "form-control"  %>
    </div>

    <%= f.submit class: "btn btn-primary" %>

<% end %>